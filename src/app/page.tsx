'use client';

import { UserButton } from '@/components/UserButton';

export default function Home() {
        return (
                <div>
                        Logged In
                        <UserButton />
                </div>
        );
}
